class AppConstants {
  AppConstants._();

  // Default currency — will be configurable in Settings screen
  static const String currency = 'NPR';

  // App name
  static const String appName = 'SpendWise';
}
